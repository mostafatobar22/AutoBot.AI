import 'package:flutter/material.dart';

class SerialPage extends StatelessWidget {
  const SerialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Serial (Coming Soon)")),
      body: const Center(
        child: Text("Serial Communication - Under Development"),
      ),
    );
  }
}